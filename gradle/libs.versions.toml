[versions]
buildconfig = "5.4.0"
moddevgradle = "2.0.80"
plugin-publish = "1.3.1"
google-auto = "1.1.0"

[plugins]
buildconfig = { id = "com.github.gmazzo.buildconfig", version.ref = "buildconfig" }
plugin-publish = { id = "com.gradle.plugin-publish", version.ref = "plugin-publish" }

[libraries]
buildconfig = { module = "com.github.gmazzo.buildconfig:plugin", version.ref = "buildconfig" }
google-auto = { module = "com.google.auto.service:auto-service", version.ref = "google-auto" }
moddevgradle = { module = "net.neoforged:moddev-gradle", version.ref = "moddevgradle" }
